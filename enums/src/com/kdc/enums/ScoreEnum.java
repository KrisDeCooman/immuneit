package com.kdc.enums;

public enum ScoreEnum {

    // enums do not use type, they use label
    // "Low", "Medium", "High", "Topscore"

    // Convention is to write the enum values in uppercase, since they are constants.
    LOW, MEDIUM, HIGH, TOPSCORE;
}
